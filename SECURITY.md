# Security Policy

## 支持的版本

| Version | Supported |
|---------|-----------|
| 0.1.x   | ✅        |
| < 0.1   | ❌        |

## 报告漏洞

**请勿在公开 Issue 中报告安全漏洞**。

请发送邮件至 **wolfs_9@hotmail.com**，包含：

- 漏洞描述
- 复现步骤（最小化）
- 受影响版本
- 可能的影响范围
- 建议的修复方案（可选）

我们承诺：

- **24 小时内** 回复确认收到
- **7 天内** 给出初步评估
- **30 天内** 发布修复版本（重大漏洞优先）
- 修复后会在 Release Notes 中致谢报告者（如不愿署名请说明）

## 已知安全建议

### 凭据管理

- 不要在代码、配置文件中硬编码数据库 / MQ 密码
- 推荐使用环境变量或 Vault / Nacos Config 等密钥管理服务
- 生产环境必须开启 MySQL SSL 和 RabbitMQ TLS

### 消息内容

- 消息 payload 会以 JSON 形式存入数据库，**勿在 payload 中包含敏感字段**（如明文密码、身份证号）
- 必要时业务侧自行加密 / 脱敏后再投递

### 多租户

- 当前版本不支持多租户隔离，多业务共用时建议拆分独立的 `transaction_message` 表或独立数据库
